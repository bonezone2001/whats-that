import { View, Text } from "react-native";
import { globalStyle } from "@styles";

export default () => {
    return (
        <View style={globalStyle.container}>
            <Text>CHAT!</Text>
        </View>
    );
}